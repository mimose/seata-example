server:
  port: 17000
  servlet:
    context-path: /order

spring:
  profiles:
    active: dev
  application:
    name: order-service
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
      config:
        server-addr: 127.0.0.1:8848
        file-extension: yaml
    alibaba:
      seata:
        tx-service-group: my_test_tx_group
